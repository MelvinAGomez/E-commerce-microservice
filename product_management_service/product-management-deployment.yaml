apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-management-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-management
  template:
    metadata:
      labels:
        app: product-management
    spec:
      containers:
      - name: product-management
        image: product-management-service:v1
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: product-management-service
spec:
  selector:
    app: product-management
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
